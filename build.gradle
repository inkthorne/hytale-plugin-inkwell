plugins {
    id 'java'
}

apply from: 'hytale-paths.gradle'

group = 'inkthorne'
version = '0.1.1-dev'

java {
    sourceCompatibility = JavaVersion.VERSION_25
    targetCompatibility = JavaVersion.VERSION_25
}

repositories {
    mavenCentral()
}

dependencies {
    compileOnly files(hytaleServerJar)
}

jar {
    archiveBaseName.set('inkwell')
    archiveVersion.set('')
}
